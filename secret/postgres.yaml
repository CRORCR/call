
# Postgres 数据库相关配置
postgres:
  default_master:
    driver: "postgres" # 连接驱动
    host: "10.4.23.178" # ip地址
    port: 5432 # 端口
    username: "duoo" # 用户名
    password: "Duoo@melot2023" # 密码
    timeout: 10 # 连接超时 单位s
    max_idle: 10 # 空闲的最大连接数
    max_open: 100 # 数据库打开的最大连接数
    read_timeout: 2 # 读超时 单位s
    write_timeout: 2 # 写超时 单位s

  default_slave:
    driver: "postgres" # 连接驱动
    host: "10.4.23.178" # ip地址
    port: 5432 # 端口
    username: "duoo" # 用户名
    password: "Duoo@melot2023" # 密码
    timeout: 10 # 连接超时
    max_idle: 10 # 空闲的最大连接数
    max_open: 100 # 数据库打开的最大连接数
    read_timeout: 2 # 读超时 单位s
    write_timeout: 2 # 写超时 单位s

  call_db:
    master:
      database: "call_db" # 数据库名
      max_idle_connections: 50 # MySQL 最大空闲连接数
      max_open_connections: 50 # MySQL 最大打开的连接数
      max_connection_life_time: 10 # 空闲连接最大存活时间，默认 10s
    slave:
      database: "call_db" # 数据库名
      max_idle_connections: 50 # MySQL 最大空闲连接数
      max_open_connections: 50 # MySQL 最大打开的连接数
      max_connection_life_time: 10 # 空闲连接最大存活时间，默认 10s
